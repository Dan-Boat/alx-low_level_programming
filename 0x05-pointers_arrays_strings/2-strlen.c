#include "main.h"
/**
* _strlen - determine strength length
* @s: character parameter pointer
* Return: c
*/
int _strlen(char *s)
{
		int c;

		for (c = 0; s[c] != '\0'; c++)
		{
				;
		}

		return (c);
}
