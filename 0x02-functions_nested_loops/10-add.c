#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: type int number 1
 * @b: type int number 2
 * Description: adds two numbers
 * Return: Always (0).
 */

int add(int a, int b)
{
	return (a + b);
}
